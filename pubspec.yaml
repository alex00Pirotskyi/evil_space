name: evil_space
description: "Evil Space Daily — a small electronic-paper coworking status page for Nha Trang."
publish_to: 'none'

version: 3.0.0+6

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  flutter_web_plugins:
    sdk: flutter

  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/content/
