name: evil_space
description: "Evil Space coworking electronic-paper web landing page for Nha Trang, Vietnam."
publish_to: 'none'

version: 2.0.0+5

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  flutter_web_plugins:
    sdk: flutter

  image: ^4.9.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/slideshow/
    - assets/content/
